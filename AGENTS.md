# 共通開発ルール

## プロジェクト

- 3人で参加する、2026年9月19日開始のハッカソン用Privateリポジトリ。
- README.md、CONTRIBUTING.md、担当Issueを読み、依頼された範囲で作業する。
- 技術スタックは未決定。実装基盤を導入したら、実際に動作確認したセットアップ・起動・テスト手順をREADME.mdに書く。

## AIツールの担当

- 詳細は `docs/ai-roles.md` を読む。
- **Claude Code**：企画書、要件定義、ロードマップ、タスク分解、GitHub Issuesでの進捗管理、発表の構成を担当する。
- **Codex**：合意済みの要件・担当Issueに基づくコーディング、環境構築、テスト、不具合修正、コードレビュー、技術文書、実装PRを担当する。
- 人間3人の役割とAIツールの役割は別。全員が両ツールを利用してよい。企画・優先順位・マージ・提出の最終判断は人間が行う。
- Claude Codeは実装タスクに目的・対象範囲・具体的な入出力・完了条件・依存関係を記載し、Codexへ渡す。
- Codexは実装結果・変更ファイル・確認方法と結果・未解決点・PRを返す。企画や優先順位、Issueの完了状態を独断で変更しない。
- Claude Codeは返却結果と人間の確認、必要なPRのマージを確認して進捗を更新する。AIの完成報告だけでは完了にしない。
- 原則として企画・進捗資料はClaude Code、実装・技術資料はCodexが更新する。ユーザーが明示的に別の作業を依頼した場合は、その依頼を優先する。
- 同じファイル・同じ作業フォルダを両AIが同時編集しない。引き継ぎはIssue・PR・コミット済み文書で行う。

## 共同作業

- 通常の変更は作業ブランチで行い、PR経由でmainへ統合する。初回のリポジトリ準備は例外。
- Codexのブランチは `codex/<担当者>-<作業内容>`、他のツールは `feat/`、`fix/`、`docs/` 等を使用できる。クラウドサービスが生成するブランチ名も許容する。
- 別のAIセッションは別ブランチ・別worktreeまたは別cloneで動かす。
- 他人の未コミット変更を上書き・破棄しない。担当外の大規模な変更は避ける。
- 同時に同じファイルやロックファイルを変更するときは、Issue・PRで担当者を決める。
- mainへのforce push、無断での履歴書き換え、リポジトリの公開化を行わない。

## 変更の確認

- 変更に関係する既存テスト・lint・buildを実行し、PRに結果を記載する。
- チェックが未整備、または実行できない場合は、その事実と理由を書く。成功したと推測しない。
- 通常のマージ前に、別メンバーが差分と動作を確認する。
- 説明・PRは原則日本語。識別子やコマンドはプロジェクトに合わせる。

## 設定・秘密情報

- APIキー、認証トークン、秘密鍵、実データの個人情報をコミットしない。
- `.env.example` には変数名・説明・空の値またはダミー値だけを書く。
- 個人のAIログイン情報を共有しない。各自のアカウントを使う。
- AIサービスの権限確認を無効化する設定を共通設定として追加しない。
