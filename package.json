{
  "name": "ai-hack-2026",
  "private": true,
  "description": "AI HACK 2026 シフト代打候補のAI選定・希望文の解釈と提出記事",
  "scripts": {
    "select": "node --env-file-if-exists=.env src/cli.mjs",
    "interpret": "node --env-file-if-exists=.env src/cli-interpret.mjs",
    "test": "node --test",
    "article:preview": "qiita preview",
    "article:new": "qiita new",
    "article:publish": "qiita publish --all",
    "arrange": "node --env-file-if-exists=.env src/cli-arrange.mjs"
  },
  "engines": { "node": ">=22.22.1" },
  "devDependencies": {
    "@qiita/qiita-cli": "^1.10.0"
  }
}
