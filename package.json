{
  "name": "ai-hack-2026",
  "private": true,
  "description": "AI HACK 2026 シフト希望文の解釈と提出記事",
  "scripts": {
    "interpret": "node --env-file-if-exists=.env src/cli.mjs",
    "test": "node --test",
    "article:preview": "qiita preview",
    "article:new": "qiita new",
    "article:publish": "qiita publish --all"
  },
  "engines": { "node": ">=22.22.1" },
  "devDependencies": {
    "@qiita/qiita-cli": "^1.10.0"
  }
}
