# 提出直前チェックリスト（9/22）

当日に考えることを減らすための手順書です。上から順に実行してください。
公式条件の出典は [公式条件のまとめ](event-brief.md) にあります。

## 動かせない事実

| 項目 | 内容 |
| --- | --- |
| **公式期限** | **9/22（火）15:00**。遅れたら審査対象外 |
| チームの提出目標 | **13:00**（期限の2時間前） |
| 提出先 | Googleフォーム。**URLはDiscordの案内を確認**（未記入なら下に貼ること） |
| 提出物① | GitHubリポジトリ（**public**）またはソース（GoogleDriveで閲覧権限） |
| 提出物② | **QiitaまたはZennの公開記事**のURL |

提出フォームのURL：＿＿＿＿＿＿＿＿＿＿（確認したらここに貼る）

## 9/22未明の確認結果（0k4c / Codex）

- 担当Issue：[提出 #65](https://github.com/0k4c/ai-hack-2026/issues/65)、[記事と実装の整合 #67](https://github.com/0k4c/ai-hack-2026/issues/67)。
- 起動・テストの対象：main `844d20c`。未マージの実装PRの結果とは分ける。
- 記事URL：[Qiitaの公開記事](https://qiita.com/ryu2002090533/items/4271b69dc53467514e59)。旧記事ID `671ad47474821c3343e4` は使用しない。
- GitHub APIではリポジトリはPublic。ログイン情報のない専用Edgeで「Sign in」「Public」とREADMEの表示を確認。
- 提出フォームURLと送信完了は未確認。別メンバーによる起動確認・2人での送信完了確認は、AIによる確認で代替しない。

| 確認 | 実行結果・範囲 |
| --- | --- |
| 秘密情報の検索 | 取得済みの全refの69コミットを `git log --all -p` で検索。下記チェックの候補0件。追加でGitHubトークン形式・秘密鍵ヘッダーも候補0件。チェックリスト自身を除外し、値を出力しない形で確認 |
| 追跡ファイル | `.env`等の秘密設定、`credentials`、`.pem`、`.key`、`output/`の追跡0件。ダミー用の `.env.example` は除外。`data/`の名簿2ファイルは架空の同じ6人 |
| 起動環境 | 新しいworktree、Windows / Node.js v24.8.0 / npm 11.19.0。`.env`・`node_modules`なし。依存パッケージはインストールしていない |
| `npm test` | 113件中112件成功・失敗0・1件スキップ。スキップはWindowsのsymlink権限に依存する既存テスト |
| `node src/compare-models.mjs --plan` | 終了0。3モデル×3回×2機能＝18回の計画を表示。実APIは呼んでいない |
| `npm run select -- --help` | 終了0。`.env`なしでヘルプを表示 |
| キー未設定の候補選定 | 子プロセスで環境変数も除去し `node src/cli.mjs` を実行。終了1、`configuration_error`、`ORCAROUTER_API_KEYを.envに設定してください。` を確認。失敗のJSONが作られる |
| `node app/serve.mjs --port 4195 --records output/playwright/startup-records` | 空の一覧から「架空サンプルを見る」で記録を表示。未送信・2巡・店長の承認待ち、承認ボタン無効をEdgeで確認。favicon.icoは404だが本文は表示できた |
| Qiitaの匿名閲覧 | 現行URLのタイトル・本文と画像4枚の読み込みを確認。公開本文には担当状況・サンプル承認の説明・録画の有無など古い記述が残る。追加修正は [PR #76](https://github.com/0k4c/ai-hack-2026/pull/76) に保存し、公開への反映は未確認 |

検索は指定したパターンと取得済み履歴の範囲です。すべての秘密情報・個人情報を検出する保証ではありません。今回は実APIの再計測・有料呼び出しをしていません。lint・buildは未導入です。確認用スクリプト・検索結果・画像はローカルの `output/playwright/`、ブラウザのスナップショットは `.playwright-cli/` に保存しています。

## 9/22 当日の時刻表

| 時刻 | やること | 担当 |
| --- | --- | --- |
| 9:00 | **機能追加を停止。** 以降は不具合修正と提出準備だけ | 3人 |
| 10:00 | 秘密情報の混入チェック（下記） | A・C |
| 10:00 | READMEの手順どおりに、**書いた本人以外**が起動できるか確認 | B |
| 11:00 | Qiita記事を公開 | C |
| 12:00 | 通し確認とデモの予備録画 | B |
| 13:00 | **Googleフォームで提出**。完了画面を2人で確認 | A＋1人 |
| 15:00 | 公式期限 | — |

**9:00の機能追加停止を守れないと落ちます。** 動いているものを確実に出すほうが、動かない新機能より点になります。

## 1. 秘密情報のチェック（10:00）

公開は取り消せません。**publicにする前に**必ず実行してください。

Git Bashでは次を実行します。1つ目は検出した行数だけを表示し、秘密値を端末や共有ログへ出さないようにします。

```bash
git log --all -p -- . ":(exclude)docs/submission-checklist.md" | grep -cE "sk-orca-[A-Za-z0-9_-]{10,}|ORCAROUTER_API_KEY=.+|QIITA_TOKEN=.+"
```

```bash
git ls-files | grep -E "^\.env$|credentials|\.pem$|\.key$"
```

- [x] 2026-09-22の確認対象で、1つ目は **0件**、2つ目は**何も出ない**（履歴・対象が変わったら再確認）

> このチェックリスト自身が検索パターンの文字列を含むため、除外しています。除外を外すと必ず自分自身にマッチします。

`grep` は未検出時に終了コード1を返します。上流の `git` にエラーが出ていないことも確認してください。

- [x] 確認対象mainの追跡ファイルに `output/` の実行記録がない
- [x] 確認対象mainの従業員データが**架空の6人**だけである
- [ ] 実在の個人情報を含むサンプルがない

**何か出た場合**：そのキーを**ローテーション（再発行）**してください。コミットを消すだけでは不十分です。そのうえで、publicにせず **GoogleDriveでのソース提出**に切り替える判断もできます。

## 2. リポジトリを公開するか決める

2026-09-22のGitHub API確認では **Public** です。この確認作業で公開設定は変更していません。公開に関する3人の合意記録は未確認です。提出方法を確認するときは次の条件を使います。

| 選択 | 条件 |
| --- | --- |
| **publicにする** | 上のチェックが全部通っていること。**3人の合意が必要**（無断公開は共通ルールで禁止） |
| **GoogleDriveでソース提出** | 公開したくない場合。閲覧権限を付けてリンクを提出する |

- [ ] どちらにするか3人で決めた
- [x] リポジトリが実際にPublicになっていることを確認した（変更した人・3人の合意記録は未確認）
- [x] **ログアウト状態でリポジトリURLが開けること**を確認した

## 3. 起動確認（10:00）

```bash
npm test
npm run select -- --help
node src/compare-models.mjs --plan
node app/serve.mjs
```

アプリの起動とテストに `npm install` は不要です。Qiita記事をローカルでプレビューするときだけ依存のインストールが必要です。最後のサーバーを起動したまま `http://127.0.0.1:4173` を開き、「架空サンプルを見る」で確認します。既存サーバーと競合するときは `node app/serve.mjs --port 4195` とし、同じポートを開いてください。

- [ ] **書いた本人以外**がREADMEの手順だけで起動できた
- [x] `npm test` の失敗が0件で、スキップの件数と理由を記録した（上の結果参照）
- [x] `.env` と環境変数の両方にキーがない状態で、キーが必要だと分かるエラーが出る

## 4. 記事の公開（11:00）

- [ ] 記事の中身が最新の実装状況と一致している（[Issue #16](https://github.com/0k4c/ai-hack-2026/issues/16)、[Issue #67](https://github.com/0k4c/ai-hack-2026/issues/67)）
- [ ] 「OrcaRouter」と「AI HACK」に触れている
- [ ] 架空データであることを明記している
- [ ] front matter の `ignorePublish` を `false` に変えた
- [ ] mainへマージし、**GitHub Actionsが成功**した
- [x] **Qiitaの画面で実際に公開されていることを確認**した（更新PRの反映確認は別。ワークフロー成功だけで判断しない）
- [x] 記事URLを控えた：[Qiitaの公開記事](https://qiita.com/ryu2002090533/items/4271b69dc53467514e59)

## 5. デモの保険（12:00）

当日に実機が落ちることがあります。

[PR #75](https://github.com/0k4c/ai-hack-2026/pull/75)にレビュー版の約42秒のブラウザ操作動画があります。通信モック・架空データ・音声なしで、CLI入力は未収録。承認統一PR #71を土台にした未マージ版のため、最終提出版の通し確認とは区別します。追加録画は [PR #78](https://github.com/0k4c/ai-hack-2026/pull/78) も確認してください。

- [ ] デモ用のサンプル入力が用意してある
- [ ] **通し1本の録画がある**（実機が落ちても説明を続けられる）
- [ ] 仮データの箇所を口頭で説明できる

## 6. 提出（13:00）

- [ ] Googleフォームに、リポジトリURL（またはDriveリンク）を入力した
- [ ] 記事URLを入力した
- [ ] 送信した
- [ ] **完了画面を2人で確認した**
- [ ] 提出した内容をDiscordかチーム内で共有した

## 提出後・9/23

最終プレゼンは**上位8チームのみ**（4分＋質疑3分）です。全チームが発表するわけではありません。

- [ ] 発表可否の連絡を確認する
- [ ] 発表する場合、4分の通し練習を2回
- [ ] 会場・受付時刻を確認し、逆算して集合する
- [ ] **移動中の開発を予定に入れない**

### 質疑で聞かれそうなこと

- AIが判断している箇所はどこか → **コードを指して説明できるように**
- 上限や権限はどう守っているか → **コードで強制している箇所を指す**
- 1件いくらかかるか → 実測値を答える
- 作らなかったものは何か、なぜか → 判断として説明する（弱点ではない）
